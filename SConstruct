#!/usr/bin/env python3

import os

# Set up the environment
env = DefaultEnvironment()

# Define default target - this is what gets built when no specific target is given
# For external applications, this should be the application itself
Default(".")